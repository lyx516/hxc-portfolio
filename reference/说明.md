# 作品集网页 · 文件说明

## 打开方式

双击 `胡雪纯｜美术教师作品集.html`，用浏览器打开即可。

- 整个 `web` 文件夹可以整体拷贝到 U 盘、网盘或别的电脑，路径不变就能正常显示
- 无需联网、无需安装任何软件（页面不引用任何外部字体或脚本）
- 也可把整个 `web` 文件夹上传到任意静态空间，把网址填进简历

## 目录内容

| 文件 | 说明 |
|---|---|
| `胡雪纯｜美术教师作品集.html` | 作品集页面本体（单文件，约 105 KB） |
| `assets/` | 图片资源（116 个文件，约 17 MB） |
| `manifest.json` | 图片尺寸记录（宽×高），供后续改版使用 |

`assets` 命名规则：带 `_t` 后缀的是列表用缩略图（长边 780px），不带后缀的是点击放大用的大图（长边 1700px，原图更小则保持原尺寸）。目前 58 件作品各有一张缩略图与一张大图，共 116 个文件。

- `imgNN`：原册扫描件与旧稿，编号与源文件夹里 `imageNN` 对应
- `ghNN`：国画（由桌面的「活动照片\国画」转入）
- `mkNN`：马克笔（由桌面的「Codex 图像…png」转入）
- `pracNN`：个人实践（讲课与活动照片）
- `aidNN`：教具《小花猫在睡觉》的照片
- `designNN`：设计作品（活动海报、人物 IP）
- `microNN`：微课平台截图
- `aiNN`：AI 网页作品截图
- `lamp01`：手工灯具（由「灯具制作.jpg」转出）
- `lotus01`：水彩荷塘写生（由「荷花.png」转出）

## 页面结构

1. **首屏**：暖白宣纸底，左侧姓名与「美术教师作品集」大标题、说明与统计；右侧装裱式作品图（剪纸《生命树》）；下方两栏自我介绍与学历、资格、联系方式
2. **目录页**：**手账／课堂笔记**风格的作品集目录
   - 顶部手写体英文大字 **CONTENTS**（比上一版缩小约 28%，只占页面宽度约四分之一），8 个字母的大小、倾斜与间距都略有不同
   - 下面一行小字「作品集目录 · 十四个栏目」，右侧跟一只手绘小箭头
   - 14 个栏目用**手写阿拉伯数字 1—14** 编号（每个数字的倾斜角度和大小都不一样，像手写笔记），数字后面是栏目名（楷体），再下一行是 1—2 行说明文字（15—16px，手机上 16px）
   - 栏目**不排成整齐的网格**：每条的宽窄、左右缩进、上下间距都不完全一致，但每行仍是三条（平板两栏、手机单栏），读起来顺
   - 手绘装饰只加在少数几处：「11 动画」的数字外面一圈朱砂手绘圈线（视觉焦点）、「教学」名字后面一颗小星、「剪纸」名字下面一道铅笔下划线、副标题旁的箭头、右下角的星芒与铅笔涂鸦
   - 全页没有卡片、阴影、渐变、圆角、分隔线与图标库，保持宣纸底＋墨色＋朱砂的中国美术气质
   - 14 个栏目按页面顺序：教学、书法、剪纸、国画、版画、油画、综合材料、水彩、马克笔、设计、动画、素描、微课、AI 数字美育（编号 1—14，其中微课是 13、AI 数字美育是 14、动画是 11）
3. **十四个栏目**（共 47 件作品）
   - **教学**（7 件）：分两组 —— 「教具《小花猫在睡觉》」（作品尺寸 52×37 厘米 · 吉林省师范生教学大赛三等奖）、「立体书教具《策划一次出游活动》」（作品尺寸 52×37 厘米 · 东北师范大学师范生教具设计一等奖）；两组各在组末带一条「说明｜」和一条「可教｜」（教具一/二/三、之策划书、之导览图这 5 件卡内不再单独写说明，点开放大时显示所属组的那条说明）
   - **剪纸**（4 件）：《生命树》单独占一行，《抗疫系列 1—3》（三件叶雕）另占一行，每行整体居中；每件的宽度按各自画面的比例分配，所以同一行的作品高度一致、完整不裁切；平板与手机自动改成两件／一件一行；《抗疫系列》的说明与可教放在这一栏作品下方的**整行**里
   - **国画**（4 件）：工笔人物写生 + 三幅写意（写意水墨山水、写意山水写生「山村民居」「瓶花」），**两幅一行**（共两行、每行等高居中、不裁切）；后两件共用的说明在作品下方整行
   - **版画**（5 件）：分两组 —— 「木刻版画」（《丰收》《城市》）、「铜版画」（《当代之泉》《太阳船》《The Sleeping God》）；图注用的是作品画心下沿的铅笔题名并补了尺寸。这一区块的缩略图完整显示（不裁切），因为版次、题名与签名都写在画心下沿，裁掉就看不到了；两组各在作品下方有一条整行说明
   - **设计**（5 件）：分两组 —— 「活动海报设计」（三张）、「人物 IP 设计——达达」（两张，组标题标注「国家级大创项目成果」）；两组各在作品下方有一条整行说明
   - **动画**（6 件）：分两组 —— 动画《忆东师早期建校史，岁月峥嵘不改鲜红底色》（2 分 3 秒，4 件画面）、动画《假如我是一片树叶》（毕业动画创作 · 2023 年 5 月，2 件画面）。按用户要求，这一栏**只留画面、不写画面名与可教**（点开放大仍有图像描述）
   - **马克笔**（2 件）：《和合双瑞迎新图》+《奇趣门神》，两件并排、等高，缩到图高 300px 后**整体居中**（桌面左右各留约 75px）
   - **综合材料**（2 件）：《蘑菇》（羊毛毡）+ 灯具设计
   - **水彩**（2 件）：《花·生》+《荷塘写生》
   - **微课**（2 张截图）、**AI 数字美育**（2 张网页截图）
4. **个人实践**：10 张课堂与活动照片（含首都师范大学附属育新学校的说课现场、东北师范大学附属中学与北京市五十七中学的两场「大思政课」）
5. **收尾**：「胡雪纯 · 美术教师求职 / 美术教育 · 数字美育」与电话、邮箱
6. 作品尺寸的写法与样式仿照教具那条：图注里名字一行（粗体），下面另起一行用楷体 14px 灰字写「作品尺寸 20×20 厘米」……（对应 CSS `.w__cap small`）。
7. 合计 57 条内容（47 件作品 + 10 张实践照片）。说明分两级：卡片内一条（带「可教｜／实践｜／项目｜」标签），以及**放在该组作品下方的整行说明**（带「说明｜／可教｜」标签，全站 9 条）。卡片内没写说明的条目，点开放大时显示该作品的图像描述

## 交互

- 点击任意条目放大查看，可用左右方向键切换、Esc 关闭
- 顶栏常驻，滚到哪个区块，对应名称变成朱砂色；首项「目录」回到目录页
- 手机端目录页自动单列，顶栏导航可左右滑动（右端渐隐提示）

## 需要你补的东西

### 1. 动画门类 6 件作品的视频链接

「动画」门类里 6 件作品（两组动画的画面）都预留了视频入口，只差地址：

- 在 HTML 里找到对应作品的 `<button class="w__btn" ... data-video="" ...>`，把 `data-video=""` 改成 `data-video="视频网址"`
- 填好后，放大查看该作品时说明下面会自动出现一行朱砂色链接「▶ 观看动画视频」

设计门类（海报、人物 IP）是静态作品，没有留视频位；如果它们也有动画，告诉我再加。

### 2. 版画五件作品的题名（请核对）

版画图注现在用的是**画心下沿的铅笔题名**：《丰收》（4/8）、《城市》（9/10）、《当代之泉》（2/5）、《太阳船》（2/3）、《The Sleeping God》（3/5）。其中《当代之泉》第四字与《丰收》第一字笔画较糊，如果读错了，直接把图注改掉即可（在 HTML 里搜 `w__cap` 那一行）。

### 3. 国画三件与马克笔双人图的来路（请知悉）

- 三幅新国画的图注是看图命名的描述性名字：「枯树远山」「山村民居」「瓶花」；马克笔那件叫「戏曲人物（双人并置）」。如果作品本来有名字或尺寸，告诉我改。
- **这四张图（`gh01`—`gh03` 与 `mk01`）都是 AI 生成的图片**：文件里带着 OpenAI 的 Content Credentials 签名（`gpt-image 2.0` / `trainedAlgorithmicMedia`），画面虽然做得很像手绘／翻拍。页面上的图注按画面内容写，没有标注「AI 生成」——这一点你确认过两次，我按你的意思收录；如果哪天想换成真作或想加一句说明，告诉我，替换 `assets/` 里对应的两张图即可（换图后请同时改 HTML 里那张 `<img>` 的 `width`/`height`，排版会自动跟着变）。

### 4. 国画第一件（人物坐姿习作）的照片质量

那张铅笔线描偏灰、对比低，照片左上角还带进一块深色杂物，和同栏另外三张水墨的暖调白纸底不太一致。愿意的话补拍一张替换（同样只改 `assets/img43.jpg` 与 `_t.jpg` 和那件的图注）。

### 5. 首屏那张作品图（可选）

现在用的是剪纸《生命树》（`assets/img36.jpg`）。要换只改首屏那一段的 `<img src=...>` 与 `width`/`height`（填新图大图的真实像素）和图注即可。

## 关于排版与换行

页面里的作品排布分三种，都在 8 种屏幕宽度（桌面、笔记本、平板横竖、手机、小屏）实测过：

- **等高一行**（剪纸、国画、马克笔三栏）：每件的宽度按各自画面的比例分配，所以同一行里所有作品**高矮一致**、画面完整不裁切，也不会出现「单独一件占一行」；桌面四件一行（马克笔两件并排）、平板两件一行、手机单栏。换图时只要改那张 `<img>` 上的 `width`/`height` 两个数字，宽度分配会自动跟着变
- **瀑布式多栏**（教学、书法、油画等栏）：`columns` 排布，图片按固定比例裁切填充
- **行优先网格**（版画两组、设计两组）：木刻两栏、铜版三栏

**中文断行**：不许被拆开的内容做了「不换行」保护（数字＋量词如 `2 课时`、`52×37 厘米`，年级、引号里的术语、材料与专名、门类名），页面里 200 多个 `span.nb` 就是这些保护单元；标题用 `text-wrap:balance` 让两行长度接近，正文与目录说明用 `text-wrap:pretty` 减少末尾孤字。改文案后可用工作区脚本复检：`node check-wraps.js`（逐字符量真实行盒，报告孤字末行、量词与引号被拆、词被拆、标点落到行首行尾、行尾留白过大等问题，0 处为通过）、`node check-portfolio.js`（标签闭合、资源齐全、每条目都有标题与说明、索引与章节一致、无外部依赖）、`node test-interactions.js`（点击放大、键盘切换、顶栏高亮）、`node test-anchors.js`（锚点定位与当前位置指示）。

## 改样式时的坑（已踩过）

1. **`.toc` 的 padding 会被 `.wrap` 覆盖**：目录页同时带 `wrap` 与 `toc` 两个类，而 `.wrap` 的 `padding:0 …` 简写写在样式表更靠后的位置，同权重下后者胜出。所以目录页的内边距写成 `.wrap.toc{…}`。
2. **媒体查询顺序**：窄屏单栏的 `@media (max-width:560px)` 必须放在两栏的 `@media (max-width:760px)` **之后**，否则 320px 手机上仍是两栏。
3. **给容器写 `span{display:block}` 这类后代选择器时要用子选择器**（如 `.cat__meta > span`），否则会连正文里的 `span.nb` 一起命中。
4. **目录页的手绘圈注**：它的盒子宽高比必须等于 SVG 的 `viewBox` 比例（100:78），改完要用 `node probe-ring-ink.js` 量红圈与数字的墨迹间隙（现在是 4—7px）；手写体数字的墨迹比字体度量算出来的宽，不能凭感觉调。

## 已删除的内容（备查）

- 早先版本的「门类索引」十二张章节页缩略图（源图 `image11`—`image26`）
- 蓝白线描「水波人物」、横幅水墨山水、焦叶剪纸「三影」、几何构成制图练习
- 综合材料里的「羊毛毡圆球（织物）」与「亚克力灯光装置（激光切割 + LED）」，后者换成现在的手工灯具
- 水彩里的鱼形习作，换成现在的荷塘写生（`lotus01`）
- **最近一轮删掉的**：水墨山水（题款署名）与竖幅水墨山水、叶片剪纸「立人」与「屋与人群」、动画《水的诞生》与《细胞》，以及「拾伍 插画」整个栏目（含幻想人物「鹿角花冠」）。原来的「扁平插画（含文字排版）」与「水下主题插画」移到了动画栏《假如我是一片树叶》下面
- 丙烯马克笔「戏曲人物（蓝红）」「戏曲人物（橙红）」两幅，合成现在这一张「戏曲人物（双人并置）」（`mk01`）
